name: Docker

on:
  push:
    branches: [develop, main]
    tags:
      - v*
  pull_request:
  schedule:
    # Weekly rebuild so the images pick up the latest tool versions and dependencies
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  test:
    name: Test build
    if: github.event_name == 'pull_request' || github.ref_name == 'develop'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        agent: [hermes, openclaw]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (no push, registry-cache backed)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.${{ matrix.agent }}
          platforms: linux/amd64
          push: false
          tags: polyglot-${{ matrix.agent }}:test
          build-args: |
            BASE_IMAGE=ghcr.io/${{ github.repository_owner }}/polyglot:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish:
    name: Publish image
    if: github.ref_name == 'main' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        agent: [hermes, openclaw]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}

      - name: Compute image tag
        id: meta
        run: |
          IMAGE_ID=ghcr.io/${{ github.repository_owner }}/polyglot-${{ matrix.agent }}
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')
          [ "$VERSION" == "main" ] && VERSION=latest

          echo "image_id=$IMAGE_ID" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.${{ matrix.agent }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.image_id }}:${{ steps.meta.outputs.version }}
          build-args: |
            BASE_IMAGE=ghcr.io/${{ github.repository_owner }}/polyglot:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
